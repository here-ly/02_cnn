data:
  batch_size: 32
  num_workers: 4
model:
  backbone: "resnet50"
  pretrained: true
optim:
  name: "adam"
  lr: 1e-3
  weight_decay: 1e-4
train:
  epochs: 100
  save_interval: 10
  log_interval: 100
seed: 42
